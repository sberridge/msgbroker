import React from "react";

type providerManagerProps = {
    authId: string
};

const ProviderManager = (props:providerManagerProps) => {

    return <div>
        <h2>Provider Management</h2>
    </div>
}

export default ProviderManager;